import { Routes } from '@angular/router';
import {NewContactFormComponent} from './new-contact-form/new-contact-form.component'

export const routes: Routes = [
    {path: 'newcontact', component:NewContactFormComponent}
];
